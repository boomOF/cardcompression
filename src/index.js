// cardсompression.js

function compress(cards) {
    // compression logic here
    return compressedсards;
    }
    
    function decompress(compressedсards) {
    // decompression logic here
    return cards;
    }
    
    module.exports = { compress, decompress };
    
    // Use the following command to publish the package to npm:
    // npm publish